const fs = require("fs"); let idx = fs.readFileSync("index.js", "utf8"); idx = idx.replace("app.use(\"/api/upload\", require(\"./routes/upload\"));", "app.use(\"/api/upload\", require(\"./routes/upload\"));\napp.use(\"/api/projects\", require(\"./routes/contributions\"));"); fs.writeFileSync("index.js", idx); console.log("index.js patched:", idx.includes("contributions") ? "OK" : "FAIL");
