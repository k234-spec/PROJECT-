# Only set this in PRODUCTION (Vercel/Netlify) to point to your deployed backend
# Leave this file empty or absent for local development (Vite proxy handles /api)
# VITE_API_URL=https://your-backend.onrender.com/api
